# WXD/IBM ONLY — DO NOT UPSTREAM. Install in the coordinator's etc/ directory.
# Uses the existing file-session-property-manager plugin; no Java default changes.
session-property-config.configuration-manager=file
session-property-manager.config-file=etc/session-property-config.json
